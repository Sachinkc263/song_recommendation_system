# Copy this file to .env and fill in values
# During development, Vite proxies /api to the backend automatically.
# Only set VITE_API_URL if you deploy frontend and backend on different origins.

VITE_API_URL=
